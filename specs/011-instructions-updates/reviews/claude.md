Стоп. Не запущу это вслепую — тут два затыка, и первый жирный (ровно §4.5, который мы в эту персону и зашивали).

## Почему formal `/speckit.review` сейчас нельзя

**1. Я — автор этой спеки.** Я написал `spec.md` два хода назад. Весь смысл `/speckit.review` и Принципа VI — **независимый, не-авторский** глаз («the model that wrote the spec is the worst auditor» — твоя же конституция). Если я напишу `reviews/claude.md`, я займу слот **внешнего** ревьюера своим self-review и **засрю твой же gate-счётчик**. Это не независимость, это подхалимаж под видом процесса.

**2. Преждевременно.** `plan.md` и `tasks.md` ещё **не существуют** (мы на clarify, plan — следующий). Formal-ревью по регламенту → 2 авто-CRITICAL за missing artifacts + смотрит только 1/3 поверхности (spec без plan/tasks). Это не ревью, это «ты пропустил шаги».

**Поэтому `reviews/claude.md` не пишу** — не корраптю Принцип VI.

## Но польза есть — авторский self-critique спеки (не gate, честно помечен)

Прогнал свою же спеку адверсариально. Нашёл реальные трещины — их лучше закрыть **до** того, как вложишься в plan:

| ID | Severity | Трещина (моя же) | Фикс |
|---|---|---|---|
| H1 | HIGH | **SC-004 (Foundation ≤90 строк/8KB) вероятно невыполним** vs FR-007 (рус.личность+англ.операционка) + FR-008 (этич.принцип) + hierarchy + boundaries. Грубый подсчёт уже ~64+ контент-строк до пробелов → 90 впритык-или-мимо. Бюджет заставит резать *обязательный* контент → внутреннее противоречие | Поднять бюджет (~120?) или явно решить, что уходит в Reference |
| H2 | HIGH | **Непроверенное несущее допущение**: FR-004 держится на том, что Claude Code резолвит `@path` в CLAUDE.md нативно. Я это **не верифицировал**. Ложно → механизм для Claude-таргета рушится | Проверить поведение Claude Code @import до plan (context7/доки) |
| H3 | HIGH | **Ты сам правишь inline-персону в CLAUDE.md руками** (наблюдал 5+ раз за сессию). FR-002 меняет её на `@import` → твой реальный workflow ломается / переезжает в Foundation. Миграция твоей текущей рус.персоны + привычки не описана | Спека должна прописать: персона правится в Foundation, CLAUDE.md держит только `@import` |
| M1 | MED | **SC-005 baseline (~951) — self-inflicted**: 951 родился от МОЕГО B2 хода (было 638). «−40% от 951» меряет от числа, что я сам раздул 2 хода назад | Абсолютный таргет или pre-B2 база |
| M2 | MED | **Две иерархии не сведены**: этич. values-hierarchy (жизнь>свобода>ресурсы>правила, FR-008) vs существующая Instruction Hierarchy персоны (safety>correctness>...). FR-014 говорит «subordinate», но не примиряет | Явно связать: values-hierarchy — под Safety/Correctness |
| M3 | MED | **FR-005 (Reference не always-loaded) зависит от неуказанного имени/места файла**. Ляжет как `*.instructions.md`/`copilot-instructions.md` → Copilot авто-грузит → нарушение FR-005 | Пин: Reference = plain linked-файл, не `.instructions.md` |
| M4 | MED | FR-003/004 целиком завязаны на **несуществующий резолвер** (@import), который вне твоих/моих рук по прошлому решению. Риск ок, но должен быть явным | Отметить как явную feasibility-зависимость |
| L1 | LOW | Принцип I: CLAUDE.md (core source) теперь зависит от `.github/instructions/` файла — самодостаточность падает (хотя персона там — санкц. исключение) | Note |

**Это самокритика, НЕ gate-вердикт.** Условно тянет на MEDIUM (несколько HIGH), но своё ревью в счёт Принципа VI не засчитываю — по определению.

## Правильная последовательность

1. Закрыть H1-H3 в спеке
2. `/speckit.plan` → `/speckit.tasks`.
